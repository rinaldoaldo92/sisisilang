//close-button by bootstrap//
<button type="button" class="close" data-dismiss="modal" aria-label="Close">
<span aria-hidden="true">&times;</span>
</button>
//close-button by bootstrap alert//
<div class="alert-edited alert-dismissible fade show" role="alert">
<p>Situs web ini mengakses cookies dari pembaca yang mengakses situs web ini. Klik <a href="<?php echo base_url("konten/aturan-dan-kesepakatan"); ?>">disini</a> untuk tahu lebih lanjut.</p> 
<button type="button" class="close" data-dismiss="alert" aria-label="Close">
<span aria-hidden="true">&times;</span>
</button>
</div>
//table class codeigniter and connected to model database//
        $this->load->model('Artikel');
        $data['halaman_item'] = $this->Artikel->tarik_halaman();

        $template = array(
        'table_open'            => '<table class="table table-striped table-responsive">',
        'table_close'           => '</table>'
        );

        $this->table->set_template($template);

        $this->table->set_heading('id', 'judul', 'slug', 'datetime', 'submitted');

        $this->load->view('pelayanrakyat/dashboard/templates/header', $data, $template);
        $this->load->view('pelayanrakyat/dashboard/halaman', $data, $template);
        $this->load->view('pelayanrakyat/dashboard/templates/footer');
//select content by id//
	public function($id)
        $data['articles'] = $this->halaman->get_where(array('id' => $id))->result_array();
//select content by slug//
	public function($slug = NULL)
        $data['halaman_item'] = $this->halaman->get_where(array('slug' => $slug))->row_array();
//tarik data PHP ke form html//
	masukkan di value untuk tag input dan diantara open dan close tag untuk tag textarea.